name: crossbow
description: The loaded game engine

publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.18.0 <3.0.0"

dependencies:
  backstreets_widgets: ^0.6.0
  cupertino_icons: ^1.0.2
  dart_sdl: ^0.10.1
  dart_synthizer: ^0.11.1
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.0.2
  intl: ^0.17.0
  jinja: ^0.3.4
  json_annotation: ^4.7.0
  path: ^1.8.2
  path_provider: ^2.0.11
  recase: ^4.1.0
  uuid: ^3.0.6
  ziggurat: ^0.51.0
dev_dependencies:
  build_runner: ^2.3.0
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter
  json_serializable: ^6.5.1

flutter:
  uses-material-design: true
